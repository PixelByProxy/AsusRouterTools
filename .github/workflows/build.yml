name: Build

on:
  push:
    branches: [ "build-workflow-1" ]

jobs:
  build:

    runs-on: ubuntu-latest
    
    env:
      BUILD_CONFIGURATION: 'Release'
      VERSION: '1.0.$GITHUB_RUN_NUMBER'

    steps:
    - uses: actions/checkout@v3
        
    - name: Print Build Number
      run: echo "Build number is $VERSION"
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
        
    - name: Restore Dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build --no-restore --configuration $BUILD_CONFIGURATION
      
    - name: Test
      run: dotnet test --no-build --verbosity normal
      
    - name: Nuget Pack
      run: dotnet pack --no-build -p:Version=$BUILD_NUMBER --configuration $BUILD_CONFIGURATION --output "${{github.workspace}}/package/" "./Lib/PixelByProxy.Asus.Router.csproj"
